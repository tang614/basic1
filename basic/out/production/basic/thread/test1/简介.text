目的：测试wait()和sleep()的区别
总结：
每一个对象都有一个监视器即锁
thread.sleep()，是本地方法(native),系统暂停当前线程，不会释放锁，把CPU的执行权让给其他线程,但是在此例中另外一个线程想要继续执行就要获取锁，它没有锁所以不能执行
但是此时主线程中的打印123不需要锁，可以执行。
object.wait(),释放锁，让线程进入等待object锁的线程池中，需要object的notify()唤醒线程